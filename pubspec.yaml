name: animation
description: A  Flutter  animation application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  flutter_markdown: ^0.5.2
  get: ^4.1.4
  lottie: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

  assets:
    - 64586-like-and-dislike-button.json
    - 65014-dog-walking.json
    - 65077-breathing-lotus.json